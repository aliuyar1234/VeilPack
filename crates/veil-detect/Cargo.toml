[package]
name = "veil-detect"
version = "0.1.0"
edition = "2024"

[dependencies]
veil-domain = { path = "../veil-domain" }
veil-extract = { path = "../veil-extract" }
veil-policy = { path = "../veil-policy" }
blake3 = "1"
serde_json = "1"

[dev-dependencies]
regex = "1"
