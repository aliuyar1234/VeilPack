# Examples

This directory contains runnable, opinionated demos that show VeilPack behavior with real inputs and expected outputs.

Available demos:
- `csv-redaction`: end-to-end CSV sanitization with a regex detector and strict policy.
- `archive-redaction`: ZIP container sanitization into canonical NDJSON.
- `email-redaction`: EML header and body sanitization into canonical NDJSON.
